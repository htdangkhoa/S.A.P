export declare enum VorbisPictureType {
    'Other' = 0,
    "32x32 pixels 'file icon' (PNG only)" = 1,
    'Other file icon' = 2,
    'Cover (front)' = 3,
    'Cover (back)' = 4,
    'Leaflet page' = 5,
    'Media (e.g. lable side of CD)' = 6,
    'Lead artist/lead performer/soloist' = 7,
    'Artist/performer' = 8,
    'Conductor' = 9,
    'Band/Orchestra' = 10,
    'Composer' = 11,
    'Lyricist/text writer' = 12,
    'Recording Location' = 13,
    'During recording' = 14,
    'During performance' = 15,
    'Movie/video screen capture' = 16,
    'A bright coloured fish' = 17,
    'Illustration' = 18,
    'Band/artist logotype' = 19,
    'Publisher/Studio logotype' = 20,
}
export interface IVorbisPicture {
    type: string;
    format: string;
    description: string;
    width: number;
    height: number;
    colour_depth: number;
    indexed_color: number;
    data: Buffer;
}
export default class Vorbis {
    static readPicture(buffer: Buffer): IVorbisPicture;
    static getPictureType(type: number): string;
}
