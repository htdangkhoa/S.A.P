import * as strtok from 'strtok2';
import { Done, GetFileSize, IStreamParser, TagCallback } from './parser';
export declare class MpegParser implements IStreamParser {
    private headerSize;
    private frameCount;
    private state;
    private audioFrameHeader;
    private bitrates;
    private offset;
    private frame_size;
    private crc;
    private stream;
    private tagEvent;
    private done;
    private readDuration;
    private fileSize;
    private frameSyncByte2;
    private calculateVbrDuration;
    constructor(headerSize: number);
    parse(stream: NodeJS.ReadableStream, tagEvent: TagCallback, done: Done, readDuration?: boolean, fileSize?: GetFileSize): void;
    end(callback: TagCallback, done: Done): strtok.Token;
    private strParse(v, cb);
    private skipSideInformation(header);
    private seekFirstAudioFrame();
    private areAllSame(array);
}
